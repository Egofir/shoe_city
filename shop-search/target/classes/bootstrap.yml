spring:
  cloud:
    config:
      uri: http://localhost:9999
      profile: shop-search,eureka-client,mq
      name: application
  rabbitmq:
    listener:
      simple:
        acknowledge-mode: manual
