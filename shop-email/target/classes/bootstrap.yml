spring:
  cloud:
    config:
      uri: http://localhost:9999
      profile: shop-email,eureka-client,mq
      name: application
  rabbitmq:
    listener:
      simple:
        acknowledge-mode: manual